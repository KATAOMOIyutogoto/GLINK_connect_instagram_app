# Instagram App Credentials (from Meta Developer Portal)
IG_APP_ID=your_instagram_app_id_here
IG_APP_SECRET=your_instagram_app_secret_here
IG_REDIRECT_URI=https://your-domain.vercel.app/api/instagram/callback
IG_SCOPES=instagram_basic,pages_read_engagement

# Encryption Key (32 bytes in base64)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY_BASE64=your_32_byte_encryption_key_in_base64

# Supabase (Required - replaces Vercel KV)
# Get these from Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================
# MIGRATION ONLY (Remove after migration)
# ============================================
# Vercel KV (only needed for migration script)
# After migration is complete, remove these lines
# KV_URL=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
